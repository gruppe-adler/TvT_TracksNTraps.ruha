class gradTnT_ctrlScrollBar {
	width = 0;
	height = 0;
	scrollSpeed = 0.06;
	arrowEmpty = QASSET(controls\scrollBar\arrowEmpty_ca.paa);
	arrowFull = QASSET(controls\scrollBar\arrowFull_ca.paa);
	border = QASSET(controls\scrollBar\border_ca.paa);
	thumb = QASSET(controls\scrollBar\thumb_ca.paa);
	color[] = {1,1,1,1};
};